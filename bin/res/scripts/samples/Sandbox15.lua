-- Sandbox15.lua
-- 注意：本文件目前只是 Sandbox13（战术战场基座）的**别名**，没有独立实现。
-- Chapter 9 formation/cooperative BT 的内容实际都在 Sandbox13 里（含 formation 层）。
-- 若要把它做成独立样本（只演示 formation/协同 BT），需新建独立 preset + 入口，见路线步骤 5「行为变更类清理」。

require("res.scripts.samples.Sandbox13")

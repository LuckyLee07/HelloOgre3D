---
name: hello-develop-design
description: Design and implement cross-module HelloOgre3D features, write requested specs or plans, or run hello-develop-design. Do not trigger for routine local fixes, general questions, or instruction-file audits.
metadata:
  short-description: HelloOgre3D feature workflow
---

# HelloOgre3D 功能开发

读取仓库根目录的 `docs/skills/workflow.md`，按用户意图选择设计、计划或实施范围，再按需加载其中的节点。遵循根 `AGENTS.md`。路径相对仓库根，不能相对用户级安装目录解释。

用户当前要求与已有授权优先于流程默认值；只在明确要求 gated 模式时逐阶段审批。沿用当前模型与可用工具。共享流程缺失时先定位正确仓库；若确实缺失，说明情况并按 AGENTS.md 和用户目标继续可完成工作。

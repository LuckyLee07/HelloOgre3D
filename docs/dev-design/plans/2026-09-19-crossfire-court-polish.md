# Crossfire 首关构图、主体与攻击阶段联合精修

状态：实现与 macOS 技术/交互回归已完成；扬声器听感、Windows 和作者主观品质评价保留。基线 `46d7fd2`。作者已授权按[后续迭代建议](2026-09-19-crossfire-next-iterations.md)继续，认领第一轮 A1–A4；保持三关、双机能力、物理命中与胜负规则。

## 改动合同

- HUD/相机：规划执行和两秒按钮保持位置；指导条压缩、底部卡片/反馈缩至 76 px，实际安全矩形改为 top96、bottom=height-104。保留首用下一步、点地移动/点敌指定和抵达自动开火说明。姓名与当前危险更紧凑，普通战斗只突出选中单位的射界，受阻定位保留完整因果。
- 主体/场景：已有包络内增强双机顶面/朝向，环境浅色从角色浅色分离，弱化地砖和边缘竞争，核心分出材料层级。原枪口、行走层、实体部署与碰撞真源不变；任何几何变化单列检查，不以bounds相同代替物理验证。
- 攻击表达：既有 Guard 发布纯值阶段进度和剩余时间，HUD显示真实蓄力/连发/冷却；新增每座哨卫一张固定复用的无碰撞炮口充能面，按实际进度亮起，开火让位实际枪焰。冷却只是开火间隔，护盾仍有效。预警沿真实锁定点，暂停同步冻结，不改数值/AI决策。
- 声音：审查已有事件/优先级/有限并发并修有证据的问题；检查可用音频工具，实际听不到时明确保留未验，不以波形或调用日志代替听感。

主会话负责 Sandbox20、Guard、Scene相机/光照、Feedback、字体、充能模块、运行/打包/文档；资产代理独占生成器与产物，HUD代理独占HUD，音频代理只读审查。共享bin/运行窗口串行，无新绑定；是否构建依据最终C++改动面判断。

## 验证

Lua5.1语法、阶段时钟/重试/引用与HUD小窗口边界；资源确定性/碰撞包络、23项查询及物理夹具；1280x720三关与960x720界面/短步回放；实际首关对照画面、连续战斗和Computer Use原生操作。运行日志读取stderr与独立stdout。与基线相同二进制时复用已验证Release；Windows、扬声器与可靠连续帧性能只有实际执行才标通过。原始媒体留tmp。

## 实施证据

### 实现与静态证据

- 相机安全区域随 HUD 缩减调整为 top=96、bottom=height-104；底部双机卡片与反馈区 76 px。标签主动避让机体点击圆，始终保留选中标记和自动开火说明；清晰射界只在规划/主动查看时显示，持续受阻提示仍保持。
- 单位顶面加强青绿 V 标、钢蓝双条与哨卫红肩；环境采用独立灰钢材质，减弱边缘与重复地砖纹理，核心顶冠和基座区分明暗。31 个模型包络与枪口坐标不变；三种角色重排几何，28 个静态网格的完整顶点/三角集合不变。细节、确定性和碰撞 profile 见 `media/models/crossfire/SOURCE.md`。
- Guard 新增纯值 `phaseProgress` / `phaseRemainingMs`；与提交版动作对拍，4 类场景共 720 tick 的状态、锁定对象、发弹时刻和瞄准点完全相同。HUD 显示真实蓄力/连发/冷却，倒计时向上取到 0.1 秒；护盾命中徽标与阶段条分开。
- `crossfire_signals.lua` 每座哨卫仅分配一个无碰撞平面（每关 1/1/2）；按真实炮口位置显示四档充能、冷却三短线，开火阶段隐藏，避免混成假枪焰。重试复用，切关先断引用再清对象；暂停使用不变的仿真状态，胜负立即隐藏。
- 修复声音三处事实错误：音量直接更新 master，零音量立即停止在播声音；拒绝的实时命令不再播放确认声；同帧盾挡与命中按最后物理事件同时选声音/特效/声像。默认总音量系数保持一致。Lua 闭包复现和实际 C++ 后端（模拟 macOS 输出，ASan/UBSan）验证通过，未据此声称听感通过。
- Lua 5.1 语法、HUD 7 组共 1325 个状态的 960/1280 布局、固定信号池/生命周期 mock、字体 322 字覆盖与资源确定性通过；`git diff --check` 通过。无 C++、绑定或工程配置改动，复用已验证的 macOS arm64 Release，SHA-256 `b103c4248319c3d84adf758e40e11cec4eb5970d56700f4b0dfa137bdb48fd09`。

### 实机证据

- 1280×720 独立包 `controls`：三关左键路线与交叉指定均自然通关，导航、结果过渡、记录重载、真实攻击阶段与每场固定信号数通过。后台模式禁硬件输入；该证据不冒充原生鼠标操作。
- 同分辨率 v6/v7 首关实机对比：设施浅边和地砖对比降低，双机标记保留更大连续色块；底栏从 100 px 缩为 76 px 后重新适配镜头。阶段文字及进度、间断预警线与真实枪焰可见。保留现有灯光参数，本轮材料调整已形成明暗层级，不额外叠加全场压暗。
- 960×720 `interface` / `pacing`：选关/帮助/设置遮罩、记录重载、两秒推进、菜单与重试取消计时、胜利复位通过；图像确认底部完整且地图可用。移动中的三处重复自动开火文案再收为一处常驻合同，其余两行显示动作和鼠标/执行提示。
- `queries` 23 项、`physics` 7 项物理夹具通过；正面进攻正常失败、失联后重试复位通过。改形角色未改变实际伤害、盾向和遮挡行为。
- Computer Use 原生窗口，1280×720：左键开始、点地面规划两侧路线、点击 ROOK 标签选中；按钮与 E 各推进两秒并自动暂停（2046/4059 tick），显示蓄力倒计时和冷却，恢复执行后 10.131 s 收复、2/2 存活、60 损伤、18 次有效侧击/18 次正面挡弹。R 重试后设置内 75%→0%→75%、静音开关与保存状态可见。这里验证原生输入和画面，不声明听到音频。
- 炮口信号实机二次修正：初稿 .70 m/细环在 720p 下仅约 8–10 px，难以区别枪管；最终 .90 m、加粗暖白中心的橙金环，以及更亮的青白三短线，保留深度检测与真枪口锚点。7.8 s / 8.3 s 图像能直接区分环状蓄力与三条冷却；修改后完整自然胜利/暂停/重试再次通过。
- 最终版连续保存 230 帧、每 100 ms 一帧，覆盖移动/转向/暂停恢复/点射/盾挡/有效命中/击毁/重试，并合成 23 s 无声 H.264 片段。该片段是 10 fps 渲染采样，不是声音或连续帧性能证明。
- 已读独立 stdout/stderr 与包内 Sandbox.log，无 Lua/Ogre 运行错误。stderr 仅保留与 v6 相同的 Apple GL `gldCopyBufferSubData: NEEDS IMPLEMENTATION` 单次提示；不把它描述成空日志。

本机证据：`tmp/crossfire-gate-20260919-165937-j8osc7df`（三关）、`170127-5cdz263z`（界面）、`170150-2z4z_ou2`（短步）、`170220-e476__7c`（查询）、`170229-5t6aa_19`（物理）、`170244-ovmv0cwb`（失败）；后五项同属 `tmp/crossfire-gate-20260919-` 前缀。最终连续画面为 `tmp/crossfire-gate-20260919-171058-4sqzoisu`；原生日志与无声片段在 `tmp/crossfire-court-polish-20260919/`。独立包为 `tmp/Crossfire-court-polish-20260919-v7.zip`（153.3 MiB）；299 个打包源码/资源文件逐项一致、codesign 和 ZIP CRC 通过，不携带开发者记录/日志。片段 `tmp/crossfire-court-polish-20260919/court-v7-battle.mp4` 使用 macOS AVFoundation 编码，230 帧/23 s/1280×720/H.264/无声，并解码抽帧核对方向与颜色。

原始日志、截图和复现脚本仅在本机 `tmp/` 与 `/private/tmp/crossfire-a4-audio/`，不入库。扬声器音色/混音主观品质、Windows 与可靠连续帧性能未验；作者主观美术和手感评价仍开放。
